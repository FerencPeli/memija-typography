export class DateUtility {
    /**
     * Get full year value.
     * @returns {Number} Current date full year value.
     */
    static GetFullYear(as) {
        return new Date().getFullYear();
    }
}
